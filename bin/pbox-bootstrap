#!/usr/bin/env bash

apt-get update

# for building vbox guest additions
apt-get install build-essential gcc dkms xorg

# for bootstrapping ansible
apt-get install python-pip python-dev software-properties-common libffi-dev libssl-dev libyaml-dev

# ansible won't install without these
pip install markupsafe requests 

pip install ansible
